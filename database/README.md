# Vidyut Setu Database Layer

This folder keeps all database-related assets separate from app code.

## Local Mongo via Docker

```bash
docker compose up -d
```

The service starts MongoDB on `localhost:27017` with DB name `sauryasetu`.
